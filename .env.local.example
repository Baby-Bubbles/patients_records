# Configuração do Supabase
# Copie este arquivo para .env.local e preencha com suas credenciais

# URL do seu projeto Supabase
# Encontre em: Settings > API > Project URL
NEXT_PUBLIC_db2_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave pública (anon key) do Supabase
# Encontre em: Settings > API > Project API keys > anon public
NEXT_PUBLIC_db2_SUPABASE_PUBLISHABLE_KEY=sua_chave_publica_aqui

# Vercel Cron Secret
# Gere com: openssl rand -base64 32
# Esta secret deve ser configurada no Vercel Dashboard (Project Settings > Environment Variables)
# Vercel automaticamente inclui isso no header Authorization ao chamar os endpoints de cron
CRON_SECRET=your_secure_random_string_here
